<!-- BEGIN CLAVAIN CODEX TOOL MAP -->
## Sylveste operating contract

Managed by the selected Clavain installation. {{INSTALLATION_RECEIPT}}

{{HOST_ADAPTER}}

For substantive work, read the selected `clavain:using-clavain` router and selected `docs/canon/reasoning-routing.md`; read applicable installed skill bodies before use; all unique specialists remain eligible. One-line behavior edits and plan execution are substantive; trivial requests are not. Reuse valid context; refresh changes.

Apply Observe → Orient → Decide → Act → Reflect → Compound → Synthesize without forced headings. Act on evidence, verify, and reconcile; compound in authorized artifacts only. Memory writes need permission.

Record an accountable JSON decision context with `reasons` and nonempty `rationale`; settled routine work records `reasons: []`. The five reasons are `unresolved-success-criteria`, `foundational-invariants`, `broad-consequences`, `difficult-verification`, and `capability-failure`. Domain labels do not elevate. Substantial new game, agent, AI/ML, graph, or product-strategy capabilities need frontier planning through changing investigation.

Resolve the actual role using `ic --json route dispatch --policy={{POLICY_PATH}} --role=<role> --context-file=<json>`; execute governed work through packaged `scripts/dispatch.sh --role <role>` with the same `CLAVAIN_ROUTING_POLICY` and `CLAVAIN_DECISION_CONTEXT`. Unsupported `--role` resolution remains an open gate; legacy `--type` or `--tier` output cannot satisfy it. `CLAVAIN_POLICY_PROFILE=ci-campaign-pilot` requires `scope: mk-ag2s` and applies only there. Keep policy unchanged.

Use one frontier author ordinarily. Foundational or especially consequential plans need an independent other-frontier reviewer; pass `--producer-identity` from the actual producer receipt. Never self-review, downgrade frontier work, or evade blocked review. Two demonstrated capability failures request escalation; a disproven premise escalates immediately. Authentication, quota, permissions, timeout and infrastructure failures are operational. Retain receipts, policy hash, classifications, exclusions, profile, actual model/effort, retries, usage, outcomes and defects in attribution. Calibration cannot relax frontier requirements, reviewer independence, quality floors, or authority gates.

User intent and runtime restrictions govern. Restrictions on dispatch do not remove authorized local guidance reads or decision recording. Continue authorized work while preserving independent review, fresh verification, release/publication approval, and empirical acceptance. A push does not authorize publication. Missing companions do not authorize installation. No broad installer or automatic refresh for a narrow instruction update. Unknown evidence stays unknown; blank identity or policy hash makes conformance unknown and acceptance false. Report unsupported host capabilities and unsatisfied gates. Configuration does not change a running parent's model; only fresh host sessions establish observed behavior. Never substitute cached green results for fresh evidence. Handoff states decisions, constraints, verification, escalation; retain playtests, experiments, user evidence and production canaries when applicable.

<!-- END CLAVAIN CODEX TOOL MAP -->
