# ML Pipeline Domain Profile

## Detection Signals

Primary signals: `models/`, `training/`, `inference/`, `datasets/`, `*.ipynb`, `dvc.yaml`, `mlflow.*`
Frameworks: PyTorch, TensorFlow, scikit-learn, HuggingFace, MLflow, W&B, DVC, Ray, Airflow
Keywords: `model.train`, `optimizer`, `loss_function`, `batch_size`, `epoch`, `learning_rate`, `embedding`

## Injection Criteria

_Placeholder — to be completed in Phase B._

<!-- When populated, this section will contain 3-5 domain-specific review bullets per core agent (fd-architecture, fd-safety, fd-correctness, fd-quality, fd-performance, fd-user-product). -->
