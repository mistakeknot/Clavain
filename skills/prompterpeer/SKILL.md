---
name: prompterpeer
description: Oracle prompt optimizer with human review. Builds high-quality prompts for GPT-5.2 Pro and shows the enhanced prompt for approval before sending. For careful, reviewed queries to Oracle.
---

# prompterpeer → interpeer (deep mode)

This skill has been merged into `interpeer`. Use `interpeer` with **deep** mode for the same workflow.

Load the `interpeer` skill and follow the **Mode: deep** section.
