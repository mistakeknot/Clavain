---
name: winterpeer
description: LLM Council review for critical decisions. Gets consensus from GPT (via Oracle) and Claude (current session) with synthesized recommendations. For multiple perspectives on important architectural or design decisions.
---

# winterpeer → interpeer (council mode)

This skill has been merged into `interpeer`. Use `interpeer` with **council** mode for the same workflow.

Load the `interpeer` skill and follow the **Mode: council** section.

Oracle references remain at `interpeer/references/`.
