# Lean startup installed acceptance

The final correctness cohort passed independent acceptance for all 12 subjects:
six scenarios on each host, including actual native compaction and resume.
Two additional fresh installed-host sessions passed independent acceptance
after publication. The canonical acceptance records are 1133 (Claude) and
1135 (Codex). See [the evidence report](rollout-acceptance.json).
`sylveste-z55b` is closed through the existing zklw signer; its
[authorization signature verifies](rollout-close.json).

Published selections are Clavain 0.6.317, Intertest 0.1.4, Interstat 0.3.7 and
Interspect 0.1.26. The three Clavain binaries and manifest come from matching
zklw builds 66 and 67 in distinct fresh guests. Publication did not rebuild
them. Independent source checks 85 and 86 passed on the final behavioral
Clavain revision.

The installed sessions used the actual selected package paths, managed
instructions and native skill catalog. Before/after observations match, the
preparation transcript prefixes match their resumed sessions, and startup
snapshots stayed fresh and unchanged. Claude emitted two session-bound startup
events reporting 1,119 serialized bytes. Its Interspect session record stayed
in an explicitly selected private directory outside the task fixture.

The acceptance covers the explicitly prompted two-phase launch. It does not
establish ordinary one-phase compliance, portable policy fallback execution,
or full-host configuration integrity. Codex retains its existing attention and
context hook wiring; `startup.py` is not a native Codex hook. The sessions
disclosed the difference between their local Sol route recommendation and the
fixed Astra/Opus execution assignments. Opus used the authorized Fable quota
exception.

Interspect's private option keeps collection active while skipping automatic
project calibration. With that option unset, an installed-hook probe confirmed
the existing project-local database behavior. The focused Python suite passed
44 tests. Fifteen of sixteen shell suites passed; the Interlab idempotency
failure also reproduces on the untouched baseline and remains tracked under
`sylveste-7aj8.9`.
The final Claude smoke also retains Interstat arithmetic errors and auxiliary
Haiku preparation usage. Installed acceptance does not imply complete collector
health or usage attribution.

All stopped cohorts, failed installed attempts, missing attribution and resume
reconciliation remain preserved. The original comparison's 6,734 sealed files
are unchanged; efficiency remains **inconclusive**. Wider distribution needs
focused compatibility checks for other supported models. `sylveste-4jmp`
remains the next separate quality sweep.

Private rollback records retain previous package selections, managed blocks
and affected skill links. Rollback is limited to those selections and blocks;
it retains evidence and unrelated repository/configuration state. Broader CI
migration tasks remain open, with no broad trigger cutover claimed.
