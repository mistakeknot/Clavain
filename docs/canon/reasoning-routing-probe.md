# Fresh-session routing probe

The previous payload embedded a rendered 0.6.314 contract and cannot establish behavior for a changed source. Build each probe from the exact reviewed Clavain source and selected policy immediately before use. Do not reuse a copied rendered block.

For an instructional contract probe, render into a temporary file without changing installed instructions:

```bash
python3 scripts/sync-agent-instructions.py --source /selected/Clavain \
  --host claude --portable-policy --render > /tmp/clavain-claude-contract-probe.md
sha256sum /tmp/clavain-claude-contract-probe.md
```

Use `--host codex` for the Codex variant. Record the source commit, policy hash, contract hash, render time, and actual model/effort receipt. A probe that merely quotes this block tests recall of supplied text; it does not prove native instruction loading. The fresh Codex and Claude behavioral canaries must observe the selected router and short canon being read in a new isolated session, its own decision context, role resolution, reviewer separation, usage/cache, and result. Re-render and restart if source or policy changes. Keep initial cache invalidation in the efficiency comparison.

Scenario for the instructional check: a new shared agent dispatch admission capability changes foundational invariants and needs replay experiments. A plan was authored by `gpt-6-astra`. Classify whether frontier planning and an independent other-frontier review are required; state whether an operational timeout is a capability strike, whether configuration changes the running parent model, and what a handoff must carry. Check the response against the current selected canon and actual routing receipt. No copied 0.6.314 answer or template counts as fresh evidence.
