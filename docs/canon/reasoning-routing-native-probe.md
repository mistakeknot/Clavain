Use only the operating instructions already loaded by your host. Do not use
tools or edit files. This is a short instruction-discovery probe, not a request
to implement work.

A new shared agent dispatch admission capability changes foundational
invariants and needs replay experiments. Its plan was authored by gpt-6-astra.

Reply with only a JSON object containing policy_sha256 and policy_selection
from your loaded managed Clavain installation receipt (use null if absent),
planning_requires_frontier, review_requires_other_frontier,
allowed_reviewer_model, operational_timeout_counts_as_capability_strike,
parent_model_changes_from_config, and a one-sentence handoff_requirement.
