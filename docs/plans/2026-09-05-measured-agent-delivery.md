# Ship measured agent delivery on this Mac

Source: the user's accepted four-lane implementation plan, executed by session
`01a0742a-d51e-7993-9db6-5a00b8595151`. Existing Beads remain the task tracker.
Implementation, scoped commits, producer pushes, and narrow Mac installation are
authorized. No public package release, zklw deployment, automatic model routing
change, or canary promotion is authorized by this rollout.

## Execution and ownership

Run three lane agents and the main integrator concurrently. Each file has one
writer. The integrator owns cross-repository integration, review disposition,
commits, pushes, and installed-consumer verification. Preserve unrelated dirty
files and divergent history. Read current instructions and producer state before
editing. Existing Autarch work requires an ownership handoff before edits.

The dependency manifest beside this plan describes sequencing, not another task
database. Authoritative enrollment, binding, acceptance, and reconciliation use
existing Intercore decisions. Exported manifests are hash-bound evidence.

## Lane 1: attribution and context experiment

Continue Sylveste-yibw's measurement slice and Sylveste-kbh5's existing canary.
Add `--task-manifest PATH` to Interstat's profiler without breaking its existing
interfaces. Retain task, session, thread, turn, response, role, model,
configuration, and evidence identities before aggregation. Add a small Clavain
enrollment/binding/report helper using existing Intercore decisions. Enrolled
dispatch selects the canonical dispatcher and explicit role; correct instructions
that select the first cached dispatcher and use only `--tier`.

Separate execution completion, independent acceptance, measurement coverage, and
pricing coverage. Deduplicate request records and reconcile cumulative turn
usage. Missing sessions, malformed/conflicting records, unfinished turns, and
impossible cache counts must produce incomplete/invalid results. Shared
coordinator requests count once in `cohort_shared`; task-exclusive amounts are
lower bounds when exact shared attribution is unavailable. Include failures,
validation, repair, and handoff preparation. Record interventions and require an
explicit active-minute estimate; do not infer it from elapsed time.

Enroll three distinct objectives before implementation dispatch: attribution,
Sylveste-8nov, and the complete Sylveste-7wn0 rollout. Manual identity receipts may
precede the helper. Label the cohort internal-tooling. Existing Autarch/Flere work
has no retroactive credit. Keep the existing 20-terminal-task/14-day canary and
human promotion gates.

At each enrolled objective's pre-acceptance checkpoint, freeze checkout, evidence,
parent session, and installed configuration. Give identical assessment prompts
and factual handoffs to a retained-history `codex fork` and a fresh session with
the same binary/model/instructions/tools/permissions/checkout. Counterbalance
order across three pairs; seal outputs before independent evaluation. If the
exact checkpoint cannot be demonstrated, prospectively assign whole tasks and
disclose weaker comparability. Never edit transcripts to manufacture checkpoints.
Measure complete usage, correctness, retained findings, rereads, integration
turns, and intervention burden. Publish even negative results. Twenty percent
savings is a target only; preserve policy if evidence is lost or correction grows.

## Lane 2: Autarch feedback pilot

Continue Sylveste-fuwn and
`apps/Autarch-feedback-pilot/docs/plans/2026-09-05-feedback-learning-loop.md`.
Until the active worker hands off ownership, perform independent acceptance
checks and send bounded findings only. Finish controller, TUI workbench,
selected-window capture, original voice notes/correctable transcripts, playback,
reconnect, retention/deletion, trace, and launch acknowledgement on macOS 26.

Require a matching recorded executable invocation before accepting a passing
retest. Preserve project and proposal-revision binding through Clavain submission
and idempotent duplicate submission. Reuse work/run/dispatch/model identities in
attribution. Lattice remains a rebuildable projection of authoritative records.
Package verified producer revisions and invoke the installed pilot.

Acceptance: record observation and voice note; correct/play evidence; reconnect
without recording loss; investigate and answer a question; review and accept a
concrete proposal; execute; launch/retest the identified build; demonstrate
accepted guidance changing later related work. Prepare the complete experience
before requesting human product judgment. Real voice input, proposal acceptance,
and retest judgment remain human evidence.

## Lane 3: governed Flere and Intercore execution

Continue Sylveste-nmiu; preserve existing RPC/dispatch-contract changes and
reconstruct the tested unlanded atomic-admission slice from retained evidence.
Strictly bind `--run-id`; reject conflicting run/scope arguments. In one SQLite
transaction check stored policy, enforced budgets, concurrency, parent
scope/depth, and dispatch insertion. Cover direct and automatic run dispatch;
preserve scheduled parameters and recheck at execution.

Generate public schemas from actual public representations. Forward backend
truthfully and reject silent substitution. Implement `flere --worker-rpc` with
explicit executable/profile/model/session identity, no ambient extensions or
configuration, and a restricted command surface. Canonical-root admission applies
to read/search/list: reject traversal and symlink escape; exclude credential and
session directories. Describe this as application-level enforcement.

Add explicit Clavain `--to flere` without changing automatic routing. Verify
effective identity/policy before prompts. A host-written terminal receipt must
bind the exact attempt and native session entry; text/quiescence cannot prove
success. Unknown outcome uses existing failed status plus
`worker_outcome_indeterminate`; prohibit automatic retry, retain evidence, append
reconciliation without rewriting terminal attempts. Keep ordinary Flere RPC
compatible with Autarch's interactive investigation and separately governed
implementation path.

## Lane 4: installer and all twenty companions

Continue Sylveste-8nov and Sylveste-7wn0. Generate/recognize supported
`[mcp_servers.<name>]` per https://learn.chatgpt.com/docs/extend/mcp. Remove global
cleanup of those tables. Only update the managed block; preserve unrelated
servers, credentials, comments, permissions, and symlinks. Reject conflicting or
malformed managed content before writing. Test isolated fixtures with the actual
invoked CLI. Broad installer execution is fixtures-only.

Review all twelve divergent skill trees: agent-native-architecture, artifact-gen,
audit, autoresearch-multi, conflict-recovery, diagnostic-maturation, dialectic,
doc-watch, interscribe, runtime-diagnostics, skill, tldrs-agent-workflow. Review
eight supporting-script targets: design-analyze, feature-report, interdoc,
interpeer, metrics, next-work, slack-messaging, working-with-claude-code. Preserve
unique/local additions; fix canonical producers, review/push, then retarget only
selected Mac links. Keep thirteen duplicate overrides and disabled skills.

After all targets, inspect fresh interactive catalogs inside/outside repositories.
Find the lowest repeatedly passing allowance in established 500-token steps,
bounded at 10000. Preserve every capability if shortening remains. Activate
companion/config changes only between recorded paired-experiment boundaries.

## Shared envelope and dependencies

Use additive enrollment identity and manifest hash, actual provider
session/thread/turn, executable, attempt identity, raw evidence references, and
aggregation semantics. Execution adapters supply evidence, never independent
acceptance. Intercore/Flere and attribution precede Clavain integration; verified
Clavain/lattice precede Autarch packaging.

## Verification and completion

Run focused regressions then required repository checks and CI. Attribution:
duplicate/conflicting records, cumulative/delta accounting, cache subsets, shared
overhead, missing coverage, failed/abandoned outcomes, deterministic replay.
Admission/RPC: last-slot races, exhausted/advisory budgets, cancellation rollback,
scope escapes, malformed/negative replies, process death around acknowledgement,
cleanup, identity mismatch, indeterminate outcomes. Autarch: Go race and Swift
tests, Clavain review and lattice reconstruction, packaged identity, full live Mac
journey. Installer: preservation/mixed config/quoted keys/symlinks/malformed TOML,
idempotence, real CLI parsing, resources, interactive discovery.

Baseline component reports (not acceptance): five focused Autarch Go packages and
two Swift tests passed; 21 Flere RPC tests passed and six worker stubs failed.
Recheck current evidence. Independent review precedes landing. Commit scoped
logical units to main, push producers before consumers. Complete only when all
four lanes are implemented, reviewed, pushed, installed where applicable, and
accepted against this plan. Preserve incomplete human/live/CI gates explicitly.

Alignment: connects human decisions, governed execution, durable evidence, and
resource accounting.

Conflict/Risk: concurrent ownership, context loss, measurement overhead; control
through single writers, explicit evidence coverage, and bounded experiments.
